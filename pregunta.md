*¿Qué es el main?*

El main en ```html5``` es el contenido `principal` y `único` de la web, siempre va debajo del header y encima del footer (es el contenido **central** de la web).

Comando *docker*:

docker run -d -p 8080:80 -v $(pwd):/usr/share/nginx/html turismo-web